using System;

namespace MobilePhone
{
    class GSMTest
    {
        static void Main()
        {
            //Console.Write("Input number of GSMs: ");
            //int gsmCount = int.Parse(Console.ReadLine());
            //GSM[] gsm = new GSM[gsmCount];
            //for (int i = 0; i < gsmCount; i++)
            //{
            //    gsm[i] = new GSM();
            //    gsm[i].InputGSMData();
            //}
            //for (int i = 0; i < gsmCount; i++)
            //{
            //    Console.WriteLine("GSM #{0}", i + 1);
            //    Console.Write(gsm[i].ToString());
            //    Console.WriteLine("===================");
            //}
            //Console.WriteLine(GSM.IPhone.ToString());

            // Krai na vsi4ko do 11 zada4a.

            // Nachalo na 12 zada4a.

            GSMCallHistoryTest test = new GSMCallHistoryTest();
            test.Testing();
        }
    }
}